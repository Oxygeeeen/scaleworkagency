import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight, BookOpen, Check } from "lucide-react";
import { PageIntro, SiteFooter, SiteHeader } from "@/components/SiteChrome";
import { insights } from "@/lib/site-data";

export const metadata: Metadata = { title: "Insights", description: "Practical resources on human evaluation, RLHF, annotation quality, model safety and selecting an AI training data partner." };
export default function InsightsPage() { return <main><SiteHeader /><PageIntro eyebrow="Insights" title="Practical notes for teams building with human data." text="Clear explanations of the operating, quality and procurement questions behind successful AI training data programmes." /><section className="section insight-directory">{insights.map((article, index) => <article id={article.slug} key={article.slug}><div className={`article-visual article-visual-${index + 1}`}><BookOpen size={32} /><span>{article.category}</span></div><div><span>{article.category} · 6 min read</span><h2>{article.title}</h2><p>{article.excerpt}</p><h3>In this guide</h3><ul>{[["Choosing useful evaluation dimensions", "Building calibration examples", "Turning human feedback into model signals"], ["Preventing instruction drift", "Sampling and layered review", "Using performance data constructively"], ["Assessing contributor operations", "Reviewing security claims", "Structuring a useful pilot"]][index].map((item) => <li key={item}><Check size={15} /> {item}</li>)}</ul><Link className="inline-link" href="/contact">Discuss the topic with us <ArrowRight size={16} /></Link></div></article>)}</section><section className="simple-cta"><div><p className="eyebrow eyebrow-light">Need a procurement resource?</p><h2>Use our capability overview in your internal review.</h2></div><Link className="button button-light" href="/capability-statement">View capability statement <ArrowRight size={17} /></Link></section><SiteFooter /></main>; }
